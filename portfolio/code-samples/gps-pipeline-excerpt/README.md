# GPS Pipeline Excerpt — WCS Processing

One representative script from the 13-script match movement pipeline: worst-case-scenario (WCS) load window processing across 10Hz GPS data.

**Status:** placeholder — add a cleaned, standalone version of the WCS processing step, with any club- or vendor-specific API details (Catapult credentials, endpoints) stripped out and replaced with a synthetic sample GPS file.

Referenced from the [Match Movement & Chain-Outcome Modelling](../../docs/projects/gps-analytics.md) page.

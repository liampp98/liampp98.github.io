# Chain Outcome Model

Logistic regression predicting chain outcomes from GPS and possession-derived features, reaching 0.956 AUC on the full match set.

**Status:** placeholder — add a cleaned-up version of the modelling script plus a `synthetic_data.csv` with fabricated player IDs and plausible (not real) match values, so the script runs end to end without touching real MFC or Champion Data.

Referenced from the [Match Movement & Chain-Outcome Modelling](../../docs/projects/gps-analytics.md) page.
